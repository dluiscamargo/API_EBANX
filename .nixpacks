[phases.setup]
nixPkgs = ["php81"]

[phases.install]
cmds = ["echo 'PHP 8.1 installed'"]

[phases.build]
cmds = ["echo 'Building PHP application'"]

[start]
cmd = "php -S 0.0.0.0:$PORT api/index.php" 